using System.Collections;
using System.Collections.Generic;
using System;
using UnityEngine;
using TMPro;

public static class Statics
{/*
    public static void OnSubmitInputFieldTMP(this TMP_InputField inputField, Callback<string> submitCallBack)
    {
            inputField.onSubmit.AddListener(text => {
                if(!inputField.wasCanceled && submitCallBack != null)
                    submitCallBack.Invoke(text);
            });
    }*/

}
